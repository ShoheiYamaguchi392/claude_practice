{
  "name": "api",
  "dockerComposeFile": ["${localWorkspaceFolder}/docker-compose.yml"],
  "service": "api",
  "workspaceFolder": "/workspace",
  "forwardPorts": [3000, 3001],
  "postCreateCommand": "pnpm install",
  "remoteUser": "bun",
  "mounts": [
    "source=${localEnv:HOME}/.claude,target=/home/bun/.claude,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.claude.json,target=/home/bun/.claude.json,type=bind,consistency=cached"
  ],
  "features": {
    "ghcr.io/devcontainers/features/git:1": {}
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "Prisma.prisma",
        "oven.bun-vscode",
        "Anthropic.claude-code"
      ],
      "settings": {
        "editor.defaultFormatter": "esbenp.prettier-vscode",
        "editor.formatOnSave": true
      }
    }
  }
}
